server:
  port: 8080
  servlet:
    context-path: /todoapp

spring:
  datasource:
    url: "jdbc:mysql://localhost:3306/todo_app"
    username: root
    password: root
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
  jwt:
    secret: secret123
    expirationMs: 86400000
